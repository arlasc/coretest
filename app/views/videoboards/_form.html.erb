<%= form_with(model: videoboard) do |form| %>
  <% if videoboard.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(videoboard.errors.count, "error") %> prohibited this videoboard from being saved:</h2>

      <ul>
        <% videoboard.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_name, style: "display: block" %>
    <%= form.text_field :product_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
